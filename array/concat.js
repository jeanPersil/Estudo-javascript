const array1 = [1, 2, 3];
const array2 = [4, 5, 6];

let juntar = array1.concat(array2); // metodo que concatena dois arrays gerando um novo array
// OBS os arrays originias nao sao mexidos, apenas é gerado um novo

console.log(juntar);
